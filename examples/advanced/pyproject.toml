[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mhcp-example-advanced"
version = "0.1.0"
description = "Advanced scanning and verdict examples for Malware Hash Checker Pro"
requires-python = ">=3.13"
license = { text = "MIT" }
dependencies = [
    "mhcp-core>=0.1.0",
    "mhcp-hashing>=0.1.0",
    "mhcp-scanner>=0.1.0",
    "mhcp-database>=0.1.0",
    "mhcp-logging>=0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
